# Security and privacy

Generated drafts and render packages can embed local paths, source media, and
downloaded provider resources. Treat them as private build artifacts.

Never commit credentials, cookies, signatures, device/account identifiers, raw
captures, signed URLs, CapCut application files, Desktop indexes, or catalogue
content. If sensitive data is published, revoke or rotate it and remove it from
Git history.
